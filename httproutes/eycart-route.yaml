apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: eycart-route
  namespace: k-ns
spec:
  parentRefs:
    - name: my-gateway
      namespace: k-ns
  rules:
    - matches: 
        - path: 
            type: PathPrefix 
            value: /ey
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: eycart-service
          port: 3000   